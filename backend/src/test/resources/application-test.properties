# Disable Kafka for tests
spring.kafka.bootstrap-servers=dummy:9092
spring.kafka.listener.auto-startup=false

# Test configurations for other services
spring.data.mongodb.uri=mongodb://localhost:27017/test-db
